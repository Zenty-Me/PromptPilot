
## 2026-09-17 列表复制按钮改为复制到剪贴板
- popup/render.js: 列表项按钮 duplicate -> copy（icon: clipboard-copy，title 复制到剪贴板）
- popup/app.js: 新增 copyTextToClipboard（navigator.clipboard.writeText + execCommand 兜底），handleAction 增加 copy 分支；duplicate 保留（toast 改为已创建副本）
- popup.html: 右键菜单拆分为「复制内容 copy」+「创建副本 duplicate」（icon copy-plus）
- popup.css: .btn-duplicate:hover 改名 .btn-copy:hover；npm test 全部通过

## 2026-09-17 智能添加站点（只填网址）
- shared/utils.js: 新增 deriveSitePattern（域名+首段路径+/*）与 deriveSiteName（主域名提取）
- 新增 shared/detector.js：DOM 探测 + 稳定 selector 生成，注册进 manifest 与 CONTENT_SCRIPT_FILES（popup 通过 executeScript 单独注入，故不能依赖其它模块）
- content/inject.js: 站点缺 inputSelector 时 0.8/2.5/5 秒重试自动补全并回写 storage，不覆盖手填值；导出 autoDetectOnce 便于测试
- popup：站点表单改为 URL 单行输入 + 「用当前标签页」+ 可折叠高级区；列表缺选择器时显示「输入框待识别」
- 新增 tests/detector-dom.test.js（8 项），已接入 npm test；六套 + dragtest 全绿
