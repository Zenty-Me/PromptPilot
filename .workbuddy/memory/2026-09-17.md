
## 2026-09-17 列表复制按钮改为复制到剪贴板
- popup/render.js: 列表项按钮 duplicate -> copy（icon: clipboard-copy，title 复制到剪贴板）
- popup/app.js: 新增 copyTextToClipboard（navigator.clipboard.writeText + execCommand 兜底），handleAction 增加 copy 分支；duplicate 保留（toast 改为已创建副本）
- popup.html: 右键菜单拆分为「复制内容 copy」+「创建副本 duplicate」（icon copy-plus）
- popup.css: .btn-duplicate:hover 改名 .btn-copy:hover；npm test 全部通过

## 2026-09-17 智能添加站点（只填网址）
- shared/utils.js: 新增 deriveSitePattern（域名+首段路径+/*）与 deriveSiteName（主域名提取）
- 新增 shared/detector.js：DOM 探测 + 稳定 selector 生成，注册进 manifest 与 CONTENT_SCRIPT_FILES（popup 通过 executeScript 单独注入，故不能依赖其它模块）
- content/inject.js: 站点缺 inputSelector 时 0.8/2.5/5 秒重试自动补全并回写 storage，不覆盖手填值；导出 autoDetectOnce 便于测试
- popup：站点表单改为 URL 单行输入 + 「用当前标签页」+ 可折叠高级区；列表缺选择器时显示「输入框待识别」
- 新增 tests/detector-dom.test.js（8 项），已接入 npm test；六套 + dragtest 全绿

## 2026-09-17 交互优化 P0（失败出口 + 已有能力接线）
- shared/utils.js: 新增 patternMatchesUrl（支持 * 通配、/* 覆盖整段路径、端口忽略）与 matchSiteForUrl（最具体 pattern 优先、跳过 enabled===false）
- popup/app.js: 新增行动条 showActionBar/hideActionBar（#action-bar）；doInject 失败 -> offerRepair() 用 matchSiteForUrl 定位当前页站点，点按钮走 申请权限 -> detectFromActiveTab -> 写回站点 -> 自动重试上次注入（lastInjectState）
- popup/app.js: handleInject(id, mode) 支持 append；右键菜单新增「追加到现有内容」；persistSite 新增站点后 suggestTabReload() 问是否刷新；markUnauthorizedSites 同时管 [data-site-grant] 按钮显隐 + grantSitePermission()；isPopupWindow() 只在 popup 里 window.close()
- popup/render.js: 标签渲染成可点 [data-tag]（点一下填进搜索框，再点取消）；站点项新增授权按钮
- popup/render.js 修了一个既有 bug：站点项里 `site.inputSelector ? ... : ...` 的三元没加括号，导致已配好选择器的站点连编辑/删除按钮一起被吞进 else 分支
- content/panel.js: 注入统一收口到 injectFromPanel(params, alreadyRelocated)，失败时就用页面里的 PromptDetector 重识别 + 写 storage，80ms 后重试一次；面板条目 Alt+点击 = append；模板注入沿用 currentPanelTemplateMode
- content/panel.js 修悬浮球定位 bug：位置只算一次，窗口拖窄后球会停在视口外。新增 applyBallPos（夹取最终 left/top 再反推 right/bottom）+ persistBallPos + resize 监听
- 新增 tests/site-match.test.js(10)、tests/popup-dom.test.js(10)、tests/panel-ball.test.js(5)；npm test 扩到九套 + dragtest，全绿（23/11/6/17/12/8/10/10/5）
